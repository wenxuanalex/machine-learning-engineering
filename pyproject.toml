[tool.ruff]
line-length = 100
target-version = "py311"

extend-exclude = [
	"eda/",
	"scripts/",
]

[tool.ruff.lint]
# Keep this minimal and practical for a learning repo.
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
